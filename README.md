# Personal Predictor

A minimalist offline-first personal prediction tool.

Upload every file in this folder to the root of your GitHub repository and replace existing files.

Do not upload the folder itself into the repo.

After uploading, open the app with `?v=4` once, delete the old iPhone Home Screen shortcut, then add it again from Safari.
